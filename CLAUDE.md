# Rollbook — 이 저장소에서 지켜야 할 것

## 절대 바꾸면 안 되는 것 — 참석자 QR 주소
- 참석자에게 나가는 안내물·명찰 QR 에 **`https://rollbook.bdokorea.workers.dev/workshop/`** 가 인쇄되어 배포된다.
  **이 주소와 이 주소가 담긴 QR 은 무슨 일이 있어도 바꾸지 않는다.**
- 따라서 다음을 건드리지 않는다:
  - `/workshop/` 경로와 그 공개 설정(`requiredRole` 의 공개 목록, `workshopView`)
  - `/workshop`·`/workshop/index.html` → `/workshop/` 301 넘김
  - Workers 배포 이름 `rollbook`(= `rollbook.bdokorea.workers.dev`), `wrangler.jsonc` 의 `name`
- 화면 안의 내용(명단·일정)은 얼마든지 바꿔도 되지만, **주소는 고정**이다.
  새 기능 때문에 경로를 옮겨야 하면 옛 주소를 그대로 살려 두고 새 주소를 덧붙인다.

## 화면 구조 (사용자가 정한 것)
- **메인 화면은 메뉴 선택 화면 `/start`** 다. 로그인하면 여기로 오고, 관리자가 주소만 쳐도 여기로 온다.
  여기서 `출석 체크 관리(/admin)` 와 `워크샵 관리(/workshop/admin)` 로 갈라진다.
- 출석 스캔 화면은 `/scan` (스캐너 권한 PC 는 `/` 가 곧 스캔 화면).
- **참석자용 워크샵 화면 `/workshop/` 은 로그인 없이 공개**되지만, 관리자 화면들에서는
  **워크샵 관리 화면의 '참석자 화면 보기' 단추로만** 연다. 다른 곳(메뉴·출석 관리·상단 바)에서
  참석자 화면으로 가는 링크를 두지 않는다. 참석자는 관리자 화면·다른 페이지에 갈 수 없어야 한다.
- **워크샵 관리 화면은 참석자가 보는 화면을 그대로 쓴다.** `직접 편집` 을 누르면 같은 모양
  (같은 탭·카드·조 목록)의 편집본으로 바뀌고, 글자를 그 자리에서 고친다. 별도 표·양식을 만들지 않는다.

## 이름·시각 규칙
- **출석부 쪽 이름은 `Rollbook` 으로 남겨 둔다** — 출석 관리(`/admin`)·스캔 화면·로그인 화면의 제목과 상단 이름.
  `Workshop` 으로 바꾼 곳은 메뉴 화면(`/start`) 하나뿐이다.
- **화면에 보이는 시각은 전부 한국시간(KST) 고정.** 저장은 UTC(`...Z`)로 하고, 보여 줄 때만
  `Asia/Seoul` 로 바꾼다 (`admin.js` 의 `kstParts`/`fmtTime`/`fmtShortTime`/`kstToday`,
  `scanner.js` 의 `fmtClock`, `workshop-admin.js` 의 `fmtKst`, `src/index.js` 의 `kstStamp`).
  보는 PC 의 시간대 설정을 따라가게 두지 않는다.

## 워크샵 앱
- `public/workshop/index.html` 은 다른 사람이 만든 단일 파일 앱. **절대 고치지 않는다.** 서버가 내보낼 때만
  HTMLRewriter 로 가공한다 (참석자용은 관리 UI 제거, 관리자용은 참석자 UI 숨기고 관리 패널·편집기를 얹음).
- 새 빌드를 받으면 파일을 통째로 교체하고 `workshop-src/` 도 같이 갱신한다. 교체 전에 `const META/PEOPLE/PROGRAM/DINNER`,
  `#adminPanel`·`#publishBtn`·`claude.use('artifact')` 갈림길이 그대로인지 확인한다.
  2026-09-10 빌드부터는 앱이 아티팩트 껍데기(`<html><head>…</head><body>` 안에 앱의 `<title>`·`<style>` 이 들어가는
  모양)로 온다 — 그래도 `.wrap`·`.tabs`·`#adminLinkBtn`·`#adminPanel`·`const RAW_TEMPLATE` 가 있으면 그대로 동작한다.
  교체 뒤에는 참석자 화면(`/workshop/`)·관리 화면·`직접 편집`·`앱 파일의 자료로 바꾸기` 를 로컬에서 한 번 돌려 본다.
- 새 파일의 자료를 참석자에게 내보내는 것은 **관리 화면 배너의 `앱 파일의 자료로 바꾸기`** 다 (누르면 이전 버전과 견준
  변동 내역이 뜬다). 실서버 DB 는 이 샌드박스에서 닿지 않으므로, 배포 뒤 사용자가 그 단추를 누르는 것으로 반영이 끝난다.
- 참석자가 보는 자료는 워크샵 DB(`ws_dataset`, 바인딩 `WSDB`)의 사용 중 버전이다. 파일에 박힌 자료는 아무것도 올리지 않았을 때의 기본값.
  **새 파일로 교체해도 사용 중인 DB 버전이 있으면 화면은 안 바뀐다** — 그럴 때 관리 화면 위에 알림이 뜨고, `앱 파일의 자료로 바꾸기` 한 번으로 전환한다.

## 속도 (건드릴 때 주의)
- **표·열을 바꾸면 `src/index.js` 의 `SCHEMA_VERSION` 을 꼭 올린다.** 안 올리면 예전 DB 가 고쳐지지 않는다.
  (워커가 새로 뜰 때마다 CREATE·ALTER 를 다 던지면 첫 요청이 느려져서, 이 값이 같으면 건너뛴다.)
- 무거운 라이브러리(xlsx·jspdf·jszip·jsqr)는 **쓸 때 불러온다** (`admin.js` 의 `loadLib`). 화면에 `<script>` 로 다시 박지 않는다.
- 워크샵 앱의 구글 글꼴 `@import` 는 화면 그리기를 막아서, **내보낼 때 빼고** `<head>` 에 비동기로 다시 단다
  (`src/index.js` 의 `wsDeferFont`). 앱 파일 자체는 그대로 둔다.
- 느릴 때는 관리자로 로그인한 채 `/api/health` 를 열어 본다 — `db_ping_ms`(데이터베이스 왕복)와 `colo`(접속 지점)가 나온다.

## 말투
- 사용자와는 **반말**로 이야기한다 (친근하게, 무례하지 않게).

## 작업 방식
- 커밋은 하되 **push(=자동 배포)는 사용자가 "배포해줘" 라고 할 때만.**
- BDO 로고는 받은 파일 그대로 쓰고 다시 그리거나 고치지 않는다.
- 커밋 메시지·코드 주석 등 저장소에 남는 곳에 모델 이름을 넣지 않는다.
- QR 포스터 규격: 격자 37칸, 로고 폭 0.60 (QR 폭의 58%). 68% 는 인식 거리가 짧아져 쓰지 않는다.
